const red = 200;
const green = 155;
const blue = 100;
const alpha = 0.8;

// const rgba = 'rgba(' + red + ',' + green + ',' + blue + ',' + alpha + ')';
const rgba = `rgba(${red}, ${green}, ${blue}, ${alpha}); `
console.log(rgba);